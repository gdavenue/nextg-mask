<section class="{{ ctaClass | default('') }} cta container">
	<h2 class="cta__title">{{ cta.title }}</h2>
	<p class="cta__desc">{{ cta.desc }}</p>
	<div>
		<a href="#" class="button button--primary button--shadow-primary">{{ cta.button.text }}</a>
	</div>
</section>